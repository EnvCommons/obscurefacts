openreward>=0.1.149
pydantic>=2.0
openai
# Search and page fetch come from openreward's WebToolset. The default
# "backsearch" backend needs nothing extra; swap to Tavily with
# OPENREWARD_SEARCH_BACKEND=tavily and uncomment the line below.
# openreward[search]
